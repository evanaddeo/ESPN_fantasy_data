[pytest]
minversion = 8.0
addopts = -q --strict-markers --cov=fantasy_ranks --cov-report=term-missing
markers =
    integration: marks tests as integration (deselect with '-m not integration')
filterwarnings =
    ignore::DeprecationWarning
